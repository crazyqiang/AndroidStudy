<resources>

    <style name="AppComposeTheme" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="textAppearanceSpan" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/result_points</item>
        <item name="android:textSize">20sp</item>
        <!-- italic、bold、normal -->
        <item name="android:textStyle">italic</item>
        <!-- monospace、serif、sans-serif -->
        <item name="fontFamily">serif</item>
    </style>

    <style name="customloadingProgressStyle">
        <item name="android:indeterminateDrawable">@drawable/loading_frame_translate
        </item>
        <item name="android:minWidth">61dp</item>
        <item name="android:maxWidth">61dp</item>
        <item name="android:minHeight">61dp</item>
        <item name="android:maxHeight">61dp</item>
    </style>

    <style name="AppTheme1" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/orange_salmon</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AnimDown" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_top_translate_in</item>
        <item name="android:windowExitAnimation">@anim/push_top_translate_out</item>
    </style>

    <style name="bg_corner">
        <item name="android:background">@drawable/popup_corner</item>
    </style>

    <style name="AnimUp" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_bottom_in</item>
        <item name="android:windowExitAnimation">@anim/push_bottom_out</item>
    </style>

    <style name="AnimHorizontal" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_scale_left_in</item>
        <item name="android:windowExitAnimation">@anim/push_scale_left_out</item>
    </style>

    <style name="AnimRight" parent="@android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/push_scale_right_in</item>
        <item name="android:windowExitAnimation">@anim/push_scale_right_out</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- Customize your theme here. -->
    </style>

    <style name="btn_style_done" parent="@style/btn_style_parent">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/btn_selector_done</item>
    </style>

    <style name="btn_style_tb_search" parent="@style/btn_style_parent">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/btn_selector_tb_search</item>
    </style>

    <style name="btn_style_cl_done" parent="@style/btn_style_cl_parent">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/btn_selector_done</item>
    </style>

    <style name="btn_style_done_new" parent="@style/btn_style_parent">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/btn_selector_done</item>
        <item name="android:layout_marginLeft">30dp</item>
        <item name="android:layout_marginRight">30dp</item>
    </style>

    <style name="btn_style_undo" parent="@style/btn_style_parent">
        <item name="android:background">@drawable/btn_selector_undo</item>
    </style>

    <style name="btn_style_parent">
        <item name="android:gravity">center</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="btn_style_cl_parent">
        <item name="android:gravity">center</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">10dp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="LoadingDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <!-- 设置背景色 透明-->
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 设置是否显示背景 -->
        <item name="android:backgroundDimEnabled">false</item>
        <!-- 设置背景透明度 -->
        <!--        <item name="android:backgroundDimAmount">0.6</item>-->
        <!-- 设置点击空白不消失 -->
        <item name="android:windowCloseOnTouchOutside">false</item>
    </style>

</resources>
